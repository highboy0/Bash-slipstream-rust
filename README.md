# Slipstream Manager 🚀

![Slipstream Banner](https://via.placeholder.com/1200x400/0d1117/ffffff?text=Slipstream+DNS+Tunnel+Manager+%F0%9F%94%92+%F0%9F%9A%80)  
**اسکریپت مدیریت حرفه‌ای و گرافیکی Slipstream برای راه‌اندازی تونل DNS-based**

[![Bash](https://img.shields.io/badge/Bash-Script-4EAA25?style=for-the-badge&logo=gnu-bash&logoColor=white)](https://www.gnu.org/software/bash/)
[![Ubuntu](https://img.shields.io/badge/Ubuntu-22.04+-E95420?style=for-the-badge&logo=ubuntu&logoColor=white)](https://ubuntu.com/)
[![whiptail](https://img.shields.io/badge/UI-whiptail-00A3E0?style=for-the-badge&logo=linux&logoColor=white)]()
[![License](https://img.shields.io/badge/License-MIT-blue?style=for-the-badge)](https://opensource.org/licenses/MIT)
[![Version](https://img.shields.io/badge/Version-1.0-brightgreen?style=for-the-badge)]()

> **Slipstream Manager** یک اسکریپت هوشمند با رابط گرافیکی (whiptail) است که تمام مراحل نصب، تنظیم و مدیریت ابزار **Slipstream** را به صورت تعاملی و آسان انجام می‌دهد.  
> دیگر نیازی به تایپ دستی دستورات پیچیده نیست — همه چیز با منوهای زیبا و دیالوگ‌باکس پیش می‌رود! ✨

## ✨ ویژگی‌های کلیدی

- 🖼 **رابط گرافیکی زیبا** با whiptail (منوها، ورودی‌ها و پیام‌های دیالوگ‌باکس)
- 📥 دانلود خودکار آخرین نسخه Slipstream از GitHub
- 🔒 ساخت خودکار گواهی self-signed
- 🔓 آزادسازی خودکار پورت 53
- ⚙️ ایجاد و مدیریت سرویس systemd (اجرای دائمی + ری‌استارت خودکار)
- 💾 ذخیره تنظیمات در فایل JSON برای ویرایش آسان
- 🌐 مدیریت Resolverها (اضافه/حذف)
- 📊 نمایش وضعیت سرویس در باکس گرافیکی
- 🗑️ حذف کامل و پاک‌سازی با تأیید دو مرحله‌ای
- 🖥️ پشتیبانی کامل از سرور خارج (kharej) و سرور ایران (iran)

## 🛠 پیش‌نیازها

- سیستم‌عامل **Ubuntu 22.04 یا 24.04** (یا هر توزیع مبتنی بر Debian)
- دسترسی **root** (برای پورت 53 و systemd)
- اتصال اینترنت

پیش‌نیازهای نرم‌افزاری به صورت خودکار نصب می‌شوند:
```bash
sudo apt update && sudo apt install whiptail curl jq openssl -y 

📦 نصب و اجرا
اسکریپت را ذخیره کنید:
sudo nano /usr/local/bin/slipstream-manager.sh
(کد کامل اسکریپت را در این ریپو کپی کنید)
مجوز اجرا بدهید:
sudo chmod +x /usr/local/bin/slipstream-manager.sh
اجرا کنید:
sudo slipstream-manager.sh
اولین بار یک پیام خوش‌آمدگویی گرافیکی می‌بینید و وارد منوی اصلی می‌شوید.
🎮 راهنمای استفاده (منوی اصلی)
پس از اجرا، این منو نمایش داده می‌شود:
گزینه مورد نظر را انتخاب کنید:
1) راه‌اندازی اولیه
2) تغییر تنظیمات
3) مدیریت Resolverها (فقط کلاینت)
4) نمایش وضعیت سرویس
5) حذف کامل Slipstream
6) خروج
1️⃣ راه‌اندازی اولیه
نوع سرور را انتخاب کنید: kharej یا iran
دامنه NS را وارد کنید (مثال: ns.example.com)
برای سرور خارج:
پورت inbound مرزبان (پیش‌فرض: 10000)
گواهی ساخته و پورت 53 آزاد می‌شود
برای سرور ایران:
پورت listen کلاینت (پیش‌فرض: 443)
Resolverها را یکی‌یکی اضافه کنید (حداقل یکی لازم است)
سرویس systemd ساخته و اجرا می‌شود
2️⃣ تغییر تنظیمات
دامنه و پورت را به‌راحتی تغییر دهید
گواهی جدید ساخته می‌شود (برای سرور خارج)
سرویس به‌طور خودکار ری‌استارت می‌شود
3️⃣ مدیریت Resolverها (فقط سرور ایران)
Resolverهای فعلی نمایش داده می‌شود
اضافه یا حذف کنید
سرویس کلاینت ری‌استارت می‌شود
4️⃣ نمایش وضعیت سرویس
خروجی کامل systemctl status در باکس گرافیکی
5️⃣ حذف کامل Slipstream
سرویس متوقف و حذف می‌شود
تمام فایل‌ها پاک می‌شوند
تأیید دو مرحله‌ای
⚙️ فایل‌های مهم
تنظیمات: /opt/slipstream/config.ini
باینری‌ها: /opt/slipstream/slipstream-server و slipstream-client
گواهی‌ها: /opt/slipstream/cert.pem و key.pem
🔧 نکات مهم و عیب‌یابی
Cloudflare: رکورد A و NS را برای ns.example.com به IP سرور خارج اشاره دهید (Proxy: DNS only)
تست DNS:
dig @ns.example.com google.com
لاگ‌ها:
journalctl -u slipstream-server.service -f
# یا
journalctl -u slipstream-client.service -f
اگر دانلود نشد: معماری سرور را چک کنید (فقط amd64). باینری را دستی از GitHub دانلود و در /opt/slipstream/ قرار دهید
❤️ ساخته‌شده برای شما
این اسکریپت برای ساده‌سازی راه‌اندازی Slipstream ساخته شده است.
اگر پیشنهادی برای بهبود (مثل پشتیبانی از چند پروفایل یا ویژگی جدید) دارید، ایشو باز کنید یا پیام بدهید!
موفق باشید و همیشه پایدار بمانید! 🚀🔥
