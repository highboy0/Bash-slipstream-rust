# Slipstream Manager 🚀

![Slipstream Banner](https://via.placeholder.com/1200x600/0d1117/ffffff?text=Slipstream+DNS+Tunnel+Manager+%F0%9F%94%92+%F0%9F%9A%80)  
*اسکریپت حرفه‌ای مدیریت Slipstream با رابط گرافیکی تعاملی (whiptail)*

[![Bash Script](https://img.shields.io/badge/Bash-Script-89e051?style=flat&logo=gnu-bash&logoColor=black)](https://www.gnu.org/software/bash/)
[![Ubuntu Compatible](https://img.shields.io/badge/Ubuntu-22.04%2B-E95420?style=flat&logo=ubuntu&logoColor=white)](https://ubuntu.com/)
[![Whiptail UI](https://img.shields.io/badge/UI-Whiptail-4ECDC4?style=flat&logo=linux&logoColor=white)]()
[![License: MIT](https://img.shields.io/badge/License-MIT-blue.svg)](https://opensource.org/licenses/MIT)
[![Stars](https://img.shields.io/github/stars/highboy0/Bash-slipstream-rust?style=social)]()
[![Forks](https://img.shields.io/github/forks/highboy0/Bash-slipstream-rust?style=social)]()

> **Slipstream Manager** یک اسکریپت کاملاً تعاملی و زیبا برای نصب، تنظیم، مدیریت و حذف ابزار **Slipstream** (تونل DNS-based ) 

### ✨ ویژگی‌ها
- 📥 دانلود خودکار آخرین نسخه Slipstream از GitHub
- 🔐 ساخت خودکار گواهی self-signed
- 🚪 آزادسازی خودکار پورت 53
- ⚙️ ایجاد و مدیریت سرویس **systemd** (اجرای دائمی + ری‌استارت خودکار)
- 💾 ذخیره تنظیمات در فایل JSON (`/opt/slipstream/config.ini`)
- 🔄 مدیریت Resolverها (اضافه/حذف)
- 📊 نمایش وضعیت سرویس در باکس گرافیکی
- 🗑 حذف کامل و پاک‌سازی ایمن
- 🌍 پشتیبانی کامل از **سرور خارج (kharej)** و **سرور ایران (iran)**

---

### ⚡ شروع سریع (Quick Start)

فقط این یک خط را در سرور خود کپی و اجرا کنید تا کل پروژه کلون شود و اسکریپت بلافاصله اجرا گردد:

```bash
git clone https://github.com/highboy0/Bash-slipstream-rust.git && cd Bash-slipstream-rust && sudo chmod +x slipstream-manager.sh && sudo ./slipstream-manager.sh
```
---

### 🛠 پیش‌نیازها
- سیستم‌عامل **Ubuntu 22.04 یا 24.04** (یا هر توزیع مبتنی بر Debian)
- دسترسی **root** (برای پورت 53 و systemd)
- اتصال اینترنت پایدار

پیش‌نیازهای نرم‌افزاری به صورت خودکار نصب می‌شوند:
```bash
sudo apt update && sudo apt install -y whiptail curl jq openssl git
```

---

### 📦 نصب دستی
1. کلون ریپو:
   ```bash
   git clone https://github.com/highboy0/Bash-slipstream-rust.git
   cd Bash-slipstream-rust
   ```

2. مجوز اجرا:
   ```bash
   sudo chmod +x slipstream-manager.sh
   ```

3. اجرا:
   ```bash
   sudo ./slipstream-manager.sh
   ```

---

### 🎮 راهنمای استفاده

پس از اجرا، پیام خوش‌آمدگویی ظاهر می‌شود و وارد **منوی اصلی** می‌شوید:

```
گزینه مورد نظر را انتخاب کنید:
1) راه‌اندازی اولیه
2) تغییر تنظیمات
3) مدیریت Resolverها (فقط کلاینت)
4) نمایش وضعیت سرویس
5) حذف کامل Slipstream
6) خروج
```

#### 1️⃣ راه‌اندازی اولیه
- نوع سرور را انتخاب کنید: **kharej** (سرور خارج) یا **iran** (سرور ایران)
- اسکریپت آخرین باینری Slipstream را دانلود می‌کند
- دامنه NS را وارد کنید (مثال: `ns.example.com`)
- **سرور خارج**:
  - پورت inbound مرزبان روی localhost (پیش‌فرض: `10000`)
  - گواهی self-signed ساخته می‌شود
  - پورت 53 آزاد می‌شود
- **سرور ایران**:
  - پورت listen کلاینت (پیش‌فرض: `443`)
  - Resolverها را یکی‌یکی اضافه کنید (حداقل یکی لازم است، مثال: `8.8.8.8:53`)
- سرویس systemd ساخته و اجرا می‌شود

#### 2️⃣ تغییر تنظیمات
- دامنه و پورت را به راحتی تغییر دهید
- گواهی جدید (در صورت تغییر دامنه) ساخته می‌شود
- سرویس به‌طور خودکار ری‌استارت می‌شود

#### 3️⃣ مدیریت Resolverها
- فقط برای سرور ایران فعال است
- Resolverهای فعلی نمایش داده می‌شود
- گزینه اضافه یا حذف Resolver

#### 4️⃣ نمایش وضعیت سرویس
- خروجی کامل `systemctl status` در یک باکس گرافیکی با قابلیت اسکرول

#### 5️⃣ حذف کامل Slipstream
- تأیید دو مرحله‌ای
- سرویس متوقف، غیرفعال و حذف می‌شود
- تمام فایل‌ها در `/opt/slipstream` پاک می‌شوند

---

### 📂 فایل‌های مهم
- `/opt/slipstream/slipstream-server` و `slipstream-client` → باینری‌ها
- `/opt/slipstream/cert.pem` و `key.pem` → گواهی‌ها
- `/opt/slipstream/config.ini` → تنظیمات ذخیره‌شده (JSON)

---





- **مشکل دانلود باینری**: اگر معماری سرور arm64 یا دیگر بود، دستی از [GitHub Releases Slipstream](https://github.com/MortezaBashsiz/slipstream/releases) دانلود کنید و در `/opt/slipstream/` قرار دهید
- **لاگ‌ها**:
  ```bash
  journalctl -u slipstream-server.service -f
  journalctl -u slipstream-client.service -f
  ```
- **به‌روزرسانی**: اسکریپت را دوباره اجرا کنید و گزینه ۱ را بزنید تا باینری جدید دانلود شود

---

### ❤️ مشارکت و پشتیبانی
- ⭐ اگر خوشتان آمد، ستاره بزنید!
- 🐛 باگ یا پیشنهاد داشتید؟ Issue باز کنید یا Pull Request بفرستید
- 💬 سؤالی دارید؟ در Issues بپرسید

**ساخته‌شده با ❤️**  
*موفق باشید!🚀*
